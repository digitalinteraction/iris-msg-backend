import { openDb, closeDb } from '../../tools/testHarness'

describe('Harmless', () => {
  
  // let db: any
  //
  // beforeEach((done) => {
  //   openDb()
  //     .then(({ db: newDb, models }) => {
  //       db = newDb
  //       done()
  //     })
  // })
  //
  // afterEach((done) => {
  //   closeDb(db).then(done)
  // })
  
  it('should pass', async () => {
    // let { db } = await openDb()
    // await new Promise(resolve => setTimeout(resolve, 3))
    // await closeDb(db)
  })
})
